
  {
    "name": "vuenodedeploy",
    "version": "1.0.0",
    "scripts": {
      "build": "npm run setup:server",
      "setup:app": "npm run setup:server && npm run setup:client",
      "setup:client": "cd VueNodeDeploy.client && npm i",
      "setup:server": "cd VueNodeDeploy && npm i",
      "start": "node VueNodeDeploy/index.js"
    },
    "engines": {
      "node": "^14.17.0"
    }
  }
  